name: example
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.10.0-75.1.beta <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  legacy_context_menu: ^2.1.2
  legacy_tree_grid:
    path: ../
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true